export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

//TODO: changes for role and role mapping
// remove this enum and use role.enum.ts
export enum Role {
  PATIENT = 'patient',
  DOCTOR = 'doctor',
  CARETAKER = 'caretaker',
  ADMIN = 'admin',
  NURSE = 'nurse',
}
